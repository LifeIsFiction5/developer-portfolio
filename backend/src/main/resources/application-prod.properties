# Prod-specific database settings
spring.datasource.url=jdbc:mysql://your-production-db-host:3306/developer_portfolio_prod
spring.datasource.username=${DB_USERNAME}  # Pull from environment variable
spring.datasource.password=${DB_PASSWORD}  # Pull from environment variable
spring.jpa.hibernate.ddl-auto=none  # In production, do not auto-update schema
